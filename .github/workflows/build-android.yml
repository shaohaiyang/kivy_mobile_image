name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          git \
          ffmpeg \
          libsdl2-dev \
          libsdl2-image-dev \
          libsdl2-mixer-dev \
          libsdl2-ttf-dev \
          libportmidi-dev \
          libswscale-dev \
          libavformat-dev \
          libavcodec-dev \
          zlib1g-dev \
          libjpeg-dev \
          libpng-dev \
          libfreetype6-dev \
          libgstreamer1.0-dev \
          gstreamer1.0-plugins-base \
          gstreamer1.0-plugins-good
        sudo apt-get install -y \
          openjdk-17-jdk \
          automake \
          autoconf \
          libtool \
          pkg-config \
          libncurses-dev \
          libtinfo6 \
          cmake \
          libffi-dev \
          libssl-dev \
          lld
    
    - name: Set up JDK
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Install Buildozer
      run: |
        pip install buildozer cython
    
    - name: Install Android SDK and accept licenses
      run: |
        # 下载并安装 Android Command Line Tools
        wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
        unzip commandlinetools-linux-9477386_latest.zip -d /tmp/cmdline-tools
        mkdir -p ~/Android/sdk/cmdline-tools/latest
        mv /tmp/cmdline-tools/cmdline-tools/* ~/Android/sdk/cmdline-tools/latest/
        
        # 设置环境变量
        export ANDROID_HOME=~/Android/sdk
        export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools
        
        # 创建许可证目录
        mkdir -p $ANDROID_HOME/licenses
        
        # 接受所有许可证
        echo "24333f8a63b6825ea9c5514f83c2829b004d1ee5" > $ANDROID_HOME/licenses/android-googletv-license
        echo "d56f5187459ea8e12826e5b7cc594f6de8866d4d" > $ANDROID_HOME/licenses/android-sdk-license
        echo "84831b9409646a918e4d6092c8cd8fb5cb37ce8e" > $ANDROID_HOME/licenses/android-sdk-preview-license
        
        # 使用 sdkmanager 安装必要的包
        sdkmanager --licenses || true
        sdkmanager "platform-tools" "platforms;android-33" "build-tools;33.0.0" "ndk;25.2.9519653" || true
        
        # 创建 Buildozer 需要的许可证目录
        mkdir -p ~/.buildozer/android/platform/android-sdk/licenses
        cp -r $ANDROID_HOME/licenses/* ~/.buildozer/android/platform/android-sdk/licenses/ || true
    
    - name: Build APK
      run: |
        # 设置环境变量
        export ANDROID_HOME=~/Android/sdk
        export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools
        
        # 先清理缓存
        buildozer android clean
        
        # 构建APK
        buildozer -v android debug
      continue-on-error: false
      timeout-minutes: 120
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: bin/*.apk
        retention-days: 90